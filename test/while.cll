d = <int,int>{ 0:0 };
dowhile:int (dget(d, 0) << 5) {
    x = dget(d, 0);
    dadd(d, 0, x+1);
    sprint('hello');
    x;
};

x = 20;
s = dowhile:string (x << 10) {
    s = 'hello' ^ ((string)x);
    sprint(s);
    s;
};
sprint(s);
